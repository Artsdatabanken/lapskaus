import React from 'react';
import AppBar from './AppBarEG'
import { storiesOf } from '@storybook/react';

storiesOf('AppBar', module)
.add('default', () => <AppBar></AppBar>)
.add('taxon', () => <AppBar></AppBar>)
